"""Individual schema migrations, one module per file, discovered and applied in filename order
by scripts/run_migrations.py. Add a new migration by dropping a new numbered module here
(0002_..., 0003_..., ...) with an `apply(conn)` function -- nothing else needs to change to pick
it up; the runner discovers modules dynamically rather than importing them by name.
"""
